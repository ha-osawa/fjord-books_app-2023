<p style="color: green"><%= notice %></p>

<div>
  <%= render @book %>
</div>

<nav>
  <%= link_to t("books.link.edit"), edit_book_path(@book) %> |
  <%= link_to t("books.link.back"), books_path %>
  <%= button_to t("books.link.destroy"), @book, method: :delete %>
</nav>
